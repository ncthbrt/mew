import Foo as X;

module Foo {
    const bar = 1;
}

module My_Lib {
  const Foo = 3; // a completely unrelated foo
  module Demo {
    const one = X::bar;
    @compute fn main() {
        return one;
    }
  }
}
